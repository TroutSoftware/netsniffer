# Overall

The files in this folder defines a series of plugins that can be used
to parse and make rules for mqtt network traffic

The inspector understands MQTT 3.1, 3.1.1 and 5.0 standards (protocol
versions 3, 4 and 5) other protocol numbers will be rejected
