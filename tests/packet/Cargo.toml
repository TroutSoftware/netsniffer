[package]
name = "test_packet"
version = "0.1.0"
edition = "2021"

[dependencies]
cxx = "1.0.0"
cxxbridge = { path = "../../cxxbridge"}

[lib]
crate-type = ["staticlib"]
path = "test_packet.rs"

[build-dependencies]
cxx-build = "1.0"
cc = "1.0"